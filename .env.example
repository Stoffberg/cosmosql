# Azure Cosmos DB Configuration
# Copy this file to .env and fill in your actual values

# Cosmos DB Connection String
# Get this from Azure Portal > Your Cosmos DB Account > Keys > Primary Connection String
COSMOS_CONNECTION_STRING=AccountEndpoint=https://YOUR-ACCOUNT.documents.azure.com:443/;AccountKey=YOUR-KEY==;

# Alternative: Use separate endpoint and key (instead of connection string)
# COSMOS_ENDPOINT=https://YOUR-ACCOUNT.documents.azure.com:443/
# COSMOS_KEY=YOUR-KEY==

# Optional: Override the test database name (default: cosmosql-test)
# TEST_DATABASE_NAME=cosmosql-test
